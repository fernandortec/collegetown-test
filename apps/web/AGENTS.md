# Agent Rules

- Prefer components to be abstracted and placed in their own files.
- Use an interface to define props for React components.
- Typography tracking must be only `tracking-normal`, `tracking-tight`, or `tracking-wide`.
- Do not use custom tracking values or extra-wide variants like `tracking-wider` / `tracking-widest`.
